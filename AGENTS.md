# AGENTS.md

本文件是 PollyCar 项目级 AI 操作协议，具有硬约束效力。

## 项目概况

- 项目名称：`PollyCar`
- 产品目的：建设安全优先、限量开放的城市行程撮合产品。
- 当前状态：`允许生产级代码实现；禁止生产启用`。
- 用户端必须保持一个 App、一个账户和乘客／车主显式身份切换；运营后台保持独立。

## 中文文档硬约束

1. 项目文档默认且必须使用简体中文。
2. 新增或修改的 Markdown 文档，其标题、正文、表格标题、说明、决策、验收标准和风险描述必须使用中文。
3. 文件名、代码标识符、命令、协议名、标准名、第三方产品名和无法准确翻译的术语可以保留英文。
4. API 字段、枚举和错误码可以使用英文，但其业务含义必须在中文文档中解释。
5. 修改文档后必须运行 `scripts/test/check-doc-governance.ps1`。

## 轻量治理

历史产品、评审和实施文档是只读基线，不构成普通开发任务。日常只维护以下最小事实源：

- `ROADMAP.md`：当前三项任务、随后任务和阻断项。
- `spec/`：已进入代码的公开 API、状态、权限和数据契约。
- `docs/decisions/`：真实资金、真实数据、生产启用、外部服务、法律合规或不可逆迁移等高风险决定。
- `DESIGN.md`：共享设计令牌和跨页面体验约束。
- `PRODUCT_LANGUAGE.md`：用户界面最终态、内部状态到用户语言的转换和产品化验收规则。

普通 UI、内部沙箱应用服务、重构、缺陷修复和测试工作直接写代码并运行最小验证；不需要新建实施包、页面清单、用户旅程、评审清单或路线图条目。

改变公开 API、状态转换、权限或数据载荷时，在同一变更中更新对应 `spec/` 与测试。只有高风险能力才需要先创建决策、评审或实施包。

以下边界持续生效：

- App 不直接连接数据库或真实第三方服务。
- 身份切换不得绕过准入、资格、配额、安全限制、权限校验或审计。
- 共享视觉规则必须来自设计令牌。
- 测试通过不能替代法律、财务、安全、隐私、公平性和最终决策人签署。

## 用户界面产品化硬约束

用户端和运营后台必须表现为可直接交付给目标用户的完整产品，不得表现为需求演示页、测试面板、状态观察器、技术控制台、接口预览或验收清单。

实现任何用户可见界面前，必须同时读取 `DESIGN.md` 和 `PRODUCT_LANGUAGE.md`。内部领域事实、接口契约和安全规则必须先转换成用户任务、可感知结果和下一步动作，不得直接复制到界面。

以下内容除非属于目标用户完成任务所必需的正式披露，否则不得直接出现在普通业务界面：

- API 字段、枚举值、错误码、状态机名称、数据库或服务名称；
- 准入策略、权限校验、配额算法、审计机制、幂等、降级、Port、Adapter 和内部命令；
- 合成数据、内部沙箱、功能门禁和生产启用状态的重复说明；
- 为证明需求已实现而添加的卡片、徽章、横幅、说明文字或只读状态字段；
- 只有开发者、测试者、评审者或决策人才能理解且用户无法采取行动的信息。

允许的例外只有：

- 法律、费用、支付、安全、隐私、授权和责任所要求的必要披露；
- 用户必须处理的阻断原因及其明确下一步；
- 运营后台目标角色履职所必需的权限、审计和风险信息；
- 非生产环境中一个克制、统一的全局环境标识，不得在各页面重复解释。

任何内部状态进入界面前，必须转换为以下信息之一：

1. 用户当前可以做什么；
2. 用户为什么暂时不能完成操作；
3. 用户下一步应该做什么；
4. 用户需要等待、重试或联系谁；
5. 对用户决定有实质影响的费用、安全、隐私或责任后果。

正常路径不得持续强调异常、限制、安全边界或系统状态。异常恢复后，对应说明必须消失。不得使用可见文案替代业务规则、权限校验、审计记录或自动化测试。

## 用户界面完成门禁

所有新增或明显改变用户可见界面的任务，在报告完成前必须执行一次产品化收敛：

1. 在目标设备或代表性视口检查最终页面，而不只检查组件、测试快照或代码；
2. 检查主任务、主行动、信息层级、正常态、空态、加载态、失败态和恢复路径；
3. 删除不影响用户理解、决定或操作的解释文字；
4. 将技术词、断言句、规则复述和内部状态替换为 `PRODUCT_LANGUAGE.md` 定义的产品表达；
5. 检查是否存在为了验收而可视化内部实现的元素；
6. 对用户 App 至少检查一个常用移动视口；对运营后台至少检查桌面视口，涉及响应式布局时再检查窄视口；
7. 在完成报告的“验证”中写明检查的页面、视口和产品化收敛结果。

如果无法运行或查看界面，任务不得声称完成产品验收；必须报告未检查的页面、阻塞原因和残余风险。

## 验证规则

- 普通变更运行目标测试、类型检查和必要构建。
- 用户可见界面变更运行目标测试、必要构建、`scripts/test/check-brand-safety.ps1` 和上述产品化完成门禁。
- 改动 `spec/` 或公开 API 时运行对应契约检查。
- 高风险、发布或跨应用变更运行 `scripts/test/preflight.ps1 -FullGovernance`。
- 日常完整回归可运行 `scripts/test/preflight.ps1`，但它不再要求复核全部历史设计文档。

无法运行时，必须报告尝试的命令、阻塞原因和残余风险。

## 路线图规则

仅在当前三项任务、随后任务、阻断项或功能门禁改变时更新 `ROADMAP.md`。普通实现过程不记录路线图，但当前任务完成时必须从“正在做”移入“最近已完成”，写明完成日期和一句验证结果；只保留最近十项。

## 高风险未来能力

以下能力必须获得明确阶段批准，并在同一变更单元中完成决策、契约、安全和验证：

- 支付、分账、钱包、储值、借贷、融资或证券。
- 身份认证、权限、合规、隐私和破坏性数据操作。
- 外部市场、跨租户、跨区域网络或生产硬件控制。

## Git 规则

- 未经要求不得暂存、提交、推送、建分支或创建 PR。
- 未经明确要求不得使用破坏性 Git 命令。
- 保留用户修改和无关工作区变化。

## 完成报告格式

```text
已完成：
- ...

验证：
- ...

路线图：
- ...

风险：
- ...

下一步：
- ...
```
