# 御驾出行 · REGO 设计系统

本文档是用户 App 的共享设计令牌、跨页面视觉规则、安全披露和可访问性约束真相源。用户界面最终态、产品语言和内部状态转换引用 `PRODUCT_LANGUAGE.md`；产品规则、权限和状态仍引用 `docs/` 与 `spec/` 的对应真相源。

核心出行页面以 `docs/product/prototypes/mobility-redesign/` 为构图与交互真相源。该原型决定 R01–R08、D01–D10、S01–S03 的地图占比、底部面板、页面级状态切换、操作层级、信息密度和乘客／车主身份语义。

两者发生冲突时按以下顺序处理：

1. 核心出行页面的构图、地图与面板关系、状态切换和身份语义服从 `mobility-redesign`；
2. 颜色、排版、间距、组件状态、安全披露和可访问性服从本文档；
3. 不逐像素复制原型，但不得将地图主场景降级为普通卡片，也不得将页面级行程状态压缩为通用卡片列表；
4. Android、iOS 和移动 Web 保持相同业务结构，允许使用符合平台习惯的视觉细节；
5. 原型中的字符占位图标不进入产品代码，产品统一使用正式图标组件。

## 设计方向

### 核心概念

> 克制的城市夜行编辑感。

御驾出行 · REGO 不是网约车效率工具、豪车炫耀产品，也不是粉色社交应用。视觉应表达：

- 有边界的城市邀请；
- 安静、可信、成熟的双向选择；
- 真实行程而非关系结果；
- 高质量但不以财富、性别或身份标签制造等级感；
- 安全规则始终清晰可见。

### 设计关键词

- 夜蓝；
- 路灯琥珀；
- 雾白；
- 编辑排版；
- 克制留白；
- 有限开放；
- 清晰状态；
- 低刺激动效。

### 明确避免

- 黑金豪车会所风；
- 粉紫恋爱滤镜；
- 高饱和网约车黄或工具蓝铺满页面；
- 用车辆价格、品牌标志或用户外貌制造层级；
- 用“抢单”“赚钱”“心动匹配”等词强化职业化或关系承诺；
- 大面积玻璃拟态、霓虹光晕和装饰性 3D。

## 品牌表达

### 双品牌架构

- 中文主品牌：`御驾出行`，负责中文用户认知、口碑传播、应用市场名称和对外内容标题；
- 英文产品品牌：`REGO`，负责 App 图标字标、启动页主视觉、登录页、英文界面和国际化产品壳；
- 联合写法：`御驾出行 · REGO`，用于官网、品牌介绍页、启动页副标题和正式品牌署名；
- 内部工程代号：`PollyCar`，仅保留在包名、数据库表、URL Scheme、代码命名空间和兼容性标识中，不再出现在普通用户界面；
- 运营后台统一使用“PollyCar 运营后台”；旧 `REGO Operations`、旧“御驾出行运营工作台”和阶段性控制台名称不再作为 Admin v2 产品标题。

### 品牌展示环境

App 只从版本化 `PublicConfig` 读取品牌展示环境：

- 本地沙箱构建生成 `sandbox`；
- 演示构建生成 `demo`；
- 正式构建必须显式提供 `production` Profile 的 `EXPO_PUBLIC_POLLYCAR_PUBLIC_CONFIG`；
- 旧品牌布尔变量和字符串变量已经删除，不再兼容读取。

| 环境 | 环境标识 | 内部技术说明 | 费用、安全与责任披露 |
| --- | --- | --- | --- |
| `sandbox` | 显示“内部沙箱”完整标识 | 完整显示合成数据、关闭能力和调试边界 | 必须显示 |
| `demo` | 显示低干扰“演示”标识 | 将“内部沙箱／合成”转换为演示语义，不展示工程代号 | 必须显示 |
| `production` | 不显示环境标识 | 隐藏纯内部技术说明，清理合成数据和工程实现措辞 | 必须显示，不得过滤或弱化 |

生产展示过滤仅适用于内部环境、合成数据和工程实现说明。以下内容在所有环境中都必须保留：

- 行程费用、是否扣款、取消责任和退款处理；
- 安全冻结、举报、申诉和紧急帮助；
- 身份验证、必要授权、隐私边界和数据保留／删除；
- 车辆审核、资格、配额和业务准入限制；
- 真实能力未启用时对用户操作结果有直接影响的关闭说明。

### 一句话定位

默认使用：

> 一种非日常的出行方式

备选文案：

> 为少数场景准备的出行选择

产品界面和传播材料不得使用“高端出行平台”“豪车接送服务”“精英出行方式”等容易制造财富等级或错误业务归类的表达。

### 品牌解释层

需要向外部机构说明产品时，统一使用：

> 御驾出行是一个提供预约制车辆出行体验的平台。

该表述是外部解释口径，不得替代产品对真实业务模式、车主自主接单、费用规则和安全责任的完整披露。

### Logo 与字标

- Logo 主视觉使用拉丁字母字标 `REGO`，以字母型标志为主；
- 使用克制的高级无衬线或细衬线方向，优先保证小尺寸清晰度；
- 主色使用夜蓝黑、深灰或低饱和暗金，不使用高亮金色；
- 禁止皇冠、龙、帝王、权杖、车标拼贴、金色光效和“土豪租车”视觉隐喻；
- 中文“御驾出行”作为品牌名称出现时，不采用书法牌匾或宫廷风字体。

### 产品语气

- 直接但不冰冷；
- 尊重用户选择；
- 不催促；
- 不制造稀缺焦虑；
- 对安全、费用、等待和失败保持诚实。

产品文案优先使用“发起行程”“查看可用车辆”“车主”“行程”，避免“立即打车”“快速匹配”“司机”和以人与人连接为卖点的表达。安全、责任、费用和必要授权不能为了“去功能化”而被隐藏。

界面文案必须遵循 `PRODUCT_LANGUAGE.md`。技术状态、安全规则和契约字段不得直接决定页面信息架构；正常路径围绕用户任务和主要行动组织，限制与异常只在实际影响当前任务时出现。

推荐：

- “当前以乘客身份使用”
- “切换到车主身份”
- “车辆资料已提交，正在审核”
- “本次没有车主接受，费用将按规则处理”

禁止：

- “解锁优质异性”
- “豪车车主专享”
- “提高邂逅成功率”
- “立即抢单”
- “付费获得更多乘客”

## 色彩系统

### 品牌与界面色

| 令牌 | 色值 | 用途 |
| --- | --- | --- |
| `color.ink.950` | `#111827` | 主文字、深色底 |
| `color.ink.800` | `#253247` | 次级深色区域 |
| `color.ink.600` | `#526078` | 次要文字 |
| `color.mist.50` | `#F7F6F2` | 主背景 |
| `color.mist.100` | `#EFEEE9` | 次级背景 |
| `color.mist.300` | `#D8D6CE` | 分隔和禁用边框 |
| `color.lamp.500` | `#C9873A` | 主强调色、关键进度 |
| `color.lamp.600` | `#A96B28` | 强调色按下态 |
| `color.jade.600` | `#267461` | 成功、有效、已批准 |
| `color.sky.600` | `#356E91` | 信息、乘客身份辅助色 |
| `color.plum.600` | `#72536F` | 车主身份辅助色 |
| `color.red.600` | `#B64A4A` | 危险、冻结、拒绝 |
| `color.white` | `#FFFFFF` | 卡片和反色文字 |

### 使用规则

- 主背景使用雾白，不使用纯白填满所有层级；
- 琥珀色只用于主要行动、关键进度和有限强调；
- 乘客与车主可以使用蓝、梅紫作为辅助识别，但身份不能只靠颜色区分；
- 成功、警告、危险必须同时包含图标、标题和文字；
- 文本与背景对比度至少满足 WCAG AA；
- 深色模式不是简单反色，必须使用独立令牌。

### 明暗主题

用户可在乘客和车主身份下自由切换明亮或暗色主题。

- 主题属于账户级界面偏好，不属于乘客或车主业务身份；
- 身份切换不得重置主题；
- 主题切换不得改变资格、配额、审核、权限或任何业务状态；
- 明暗主题都必须保留乘客城市蓝和车主克制梅紫的身份语义；
- 所有一级页面均提供可访问的主题切换入口；
- 当前首期使用内存偏好，不写入真实账户或设备敏感存储。

暗色主题令牌：

| 令牌 | 色值 | 用途 |
| --- | --- | --- |
| `dark.background` | `#0D1420` | 主背景 |
| `dark.surface` | `#151F2E` | 卡片和输入背景 |
| `dark.surfaceMuted` | `#202C3D` | 次级背景和提示 |
| `dark.border` | `#344156` | 边框与分隔 |
| `dark.text` | `#F4F1EA` | 主文字 |
| `dark.textSecondary` | `#AAB4C4` | 次要文字 |
| `dark.deepSurface` | `#080E17` | 深层主状态区域 |
| `dark.primary` | `#D69A52` | 琥珀主行动 |
| `dark.success` | `#5FA58F` | 成功和有效 |
| `dark.passenger` | `#70A5C3` | 乘客身份辅助色 |
| `dark.owner` | `#AE8AAA` | 车主身份辅助色 |
| `dark.danger` | `#D87575` | 危险、拒绝和冻结 |

暗色主题不使用纯黑，不使用高饱和霓虹，不允许简单执行颜色反转。

## 字体与排版

### 字体栈

- 中文界面：`Noto Sans SC`；
- 拉丁字符与数字：`Manrope`；
- 品牌标题和重点数字可使用 `Manrope`，正文不得以拉丁字体替代中文字形；
- 字体加载失败时回退到平台无衬线字体，并保持布局不跳变。

### 字号

| 令牌 | 字号/行高 | 用途 |
| --- | --- | --- |
| `type.display` | `32/40` | 一级工作台标题 |
| `type.title.1` | `24/32` | 页面标题 |
| `type.title.2` | `20/28` | 区块标题 |
| `type.body` | `16/24` | 正文与表单 |
| `type.body.small` | `14/20` | 次要说明 |
| `type.caption` | `12/18` | 标签与辅助信息 |
| `type.numeric` | `28/32` | 时间、配额和关键数值 |

规则：

- 正文最小字号为 14；
- 不使用全大写英文作为主要标签；
- 状态名称短而明确；
- 费用、时间和额度使用等宽数字特性；
- 一屏最多保持三个明显文字层级。

## 空间与布局

### 间距

采用 `4` 为基础单位：

`4、8、12、16、20、24、32、40、48`

- 页面水平边距：手机默认 `20`；
- 卡片内边距：`16` 或 `20`；
- 区块间距：`24` 或 `32`；
- 触控目标最小 `44 × 44`；
- 表单字段间距不得小于 `16`。

### 圆角

| 令牌 | 数值 | 用途 |
| --- | --- | --- |
| `radius.small` | `8` | 标签、输入和小按钮 |
| `radius.medium` | `14` | 卡片和弹层 |
| `radius.large` | `22` | 工作台主卡片 |
| `radius.pill` | `999` | 身份标签和状态胶囊 |

不使用所有元素统一大圆角。主要内容卡片圆角高于工具控件。

### 阴影与边界

- 默认使用浅边框和层级背景，不依赖厚重阴影；
- 浮层允许使用低透明度、宽扩散阴影；
- 审核、安全和状态页面优先使用结构分区，不使用装饰性悬浮卡片堆叠。

## 身份系统

### 身份展示

所有一级页面顶部展示：

- 当前身份文字；
- 身份状态；
- 可操作的切换入口；
- 内部沙箱标识。

乘客辅助色为 `sky.600`，车主辅助色为 `plum.600`，但同时必须使用“乘客”“车主”文字与对应图标。

### 身份切换交互

- 点击身份入口打开底部身份面板；
- 当前身份置顶并标记；
- 目标身份展示状态和待办数；
- 未开通车主身份时显示“申请车主身份”，不得伪装为可直接切换；
- 存在未提交表单或关键任务时先出现影响确认；
- 切换成功使用短暂横向过渡和标题更新，不使用整屏翻转或角色变身动画；
- 切换失败保留原身份和页面，并给出可行动错误。

## 导航

### 共享外壳

- 顶部：当前身份、页面标题、通知；
- 主体：身份工作台；
- 底部：随身份变化的四个一级入口；
- 共享入口：通知、安全、帮助、隐私和账户。

乘客导航：

- 首页；
- 行程；
- 消息；
- 我的。

车主导航：

- 接单；
- 行程；
- 资格；
- 我的。

消息能力关闭或不可用时仍保留状态说明，不显示虚假的未读或可聊天入口。

## 核心组件

### 按钮

- `PrimaryButton`：每个页面最多一个主要行动；
- `SecondaryButton`：次要但可继续的行动；
- `TextButton`：返回、稍后处理和帮助；
- `DangerButton`：撤销、取消和安全相关破坏性操作。

按钮必须包含明确动词，不使用“确定”承载所有动作。

### 状态组件

- `StatusBanner`：功能关闭、冻结、维护和沙箱状态；
- `StatusTimeline`：审核、支付、行程和申诉进度；
- `ReasonList`：补充材料、拒绝和不可用原因；
- `RecoveryActions`：重试、补充、申诉、帮助和返回；
- `IdentityBadge`：当前身份和通知归属。

### 表单

- 标签永远可见，不以占位符替代；
- 必填、说明、错误和隐私用途就近展示；
- 长表单按任务分段，不使用单屏巨大表单；
- 草稿、本地校验和服务端校验结果明确区分；
- 提交前展示材料摘要和数据用途；
- 合成附件必须显示“合成材料”，避免误传真实资料。

### 卡片

- 任务卡：突出下一步和截止时间；
- 行程卡：突出状态、时间和双方责任，不突出用户画像；
- 资格卡：展示有效性、额度和限制原因；
- 审核卡：展示最小必要信息和可执行动作。

## 首个车辆准入切片

### 申请说明页

- 顶部明确“申请车主身份”；
- 首屏说明需要完成车主和车辆审核；
- 展示三步流程：资料、车辆、审核；
- 明确内部沙箱只接受合成材料；
- 主要行动：“开始申请”。

### 车辆资料页

- 使用分组表单：车辆基础信息、权属与使用权、保险与有效期、合成附件；
- 不展示品牌白名单或车辆价格门槛；
- 不使用豪车图片作为准入暗示；
- 校验失败时定位到具体字段；
- 页面底部固定保存与继续操作。

### 审核进度页

- 使用纵向状态时间线；
- 当前状态标题优先于装饰插图；
- 等待状态说明预计动作，不承诺审核时长；
- 需补充时突出具体材料和截止条件；
- 审核通过后提供“切换到车主身份”；
- 审核拒绝展示可披露原因和申诉/重新申请条件。

### 车主工作台

- 首屏展示当前车主资格、车辆状态和真实接单关闭状态；
- 不出现虚假订单；
- 在关闭状态下说明为什么暂不可接单，以及哪些内部沙箱任务可继续；
- 资格、车辆和安全限制分别展示，不混成一个模糊状态。

## 动效

- 页面进入：`180–240ms`，轻微位移和透明度；
- 身份切换：`220ms`，共享外壳保持稳定，只更新工作台内容；
- 状态更新：`160ms`，颜色与图标同步变化；
- 加载使用骨架或局部进度，不使用无限旋转覆盖整页；
- 支持系统减少动态效果设置；
- 不使用庆祝彩带、心跳、粒子或模拟车辆疾驰动效。

## 图像与图标

- 首期以图标、地图抽象线条和城市光影纹理为主；
- 不使用真实用户照片作为默认核心视觉；
- 不用豪车特写表达价值；
- 插图必须服务于说明、安全或空状态；
- 图标统一线宽、端点和视觉尺寸。

## 可访问性

- 所有交互元素提供可访问名称和状态；
- 身份切换器朗读当前身份和目标身份状态；
- 动态状态变化通过可访问性提示通知；
- 支持系统字体缩放，关键操作不得被截断；
- 错误不只用颜色表达；
- 触控目标、焦点顺序和屏幕阅读器路径纳入组件验收。

## 设计风险

### 风险一：克制而非豪华

选择雾白、夜蓝和琥珀，而不是黑金。收益是降低财富炫耀和会所感，更符合安全、真实和有限参与；代价是第一眼不一定被理解为“豪华出行”，需要依靠产品内容说明体验价值。

### 风险二：编辑感而非效率工具

使用较大留白、清晰时间线和有限主行动，而不是高密度订单列表。收益是减少抢单和职业化暗示；代价是单位屏幕信息量较低，需要通过任务优先级和通知设计保持效率。

## 实施规则

- 所有色彩、字体、间距、圆角和动效必须从主题令牌读取；
- 页面不得直接写散落的十六进制颜色；
- 首个组件实现必须同时具备可访问性属性和状态测试；
- 新增视觉模式先更新本文件，再进入组件实现；
- 非生产环境必须保留统一全局环境标识，但业务页面不得重复展示或解释内部沙箱、合成数据和功能门禁；
- 所有真实能力关闭态必须按正式产品状态设计和测试。

## 最终态产品体验

- 页面以目标角色的主要任务组织，不以接口字段、领域实体、状态机节点或验收条目组织；
- 首屏优先呈现当前任务、关键上下文和一个主要行动；
- 正常状态不使用横幅、警告或状态卡片强调系统健康、资格、权限、配额和审计；
- 异常、限制和等待状态必须提供可行动的下一步，并在恢复后移除；
- 不得为了证明业务规则已实现而增加可见字段、徽章、说明区或状态总览；
- 业务规则由代码、契约和测试证明，界面只表达用户可感知的结果和重要后果；
- 同一事实不得通过标题、徽章、卡片、横幅和正文重复表达；
- 用户 App 保持消费级产品语气，运营后台保持业务工作台语气，二者都不得呈现为开发工具。

## 信息密度与内容层级

- 普通页面默认只有一个页面标题、一个阶段任务、一个强调级主要操作和最多一个主要阻断；
- 辅助说明应靠近对应操作，不建立独立“规则说明”“当前边界”或“技术状态”区域；
- `StatusBanner` 只用于当前任务真实发生的重要阻断、风险或恢复结果，不用于长期展示正常事实；
- `Badge` 只表达用户需要快速识别的业务状态，不展示内部角色、权限、环境、策略或枚举；
- 卡片用于组织用户对象和业务任务，不得一项需求对应一张卡片；
- 空态、加载态、失败态和等待态的语言结构遵循 `PRODUCT_LANGUAGE.md`。

## 截图与文案收敛验收

新增或明显改变用户可见页面后，必须在代表性视口执行一次独立于代码测试的产品化验收：

1. 截图或目视检查首屏、主要操作完成后状态以及最重要的失败、空态或等待状态；
2. 检查主要行动是否清晰，异常和内部说明是否抢占正常任务；
3. 删除不影响用户理解、决定或操作的文字；
4. 删除重复环境说明、状态断言、规则复述和实现解释；
5. 将错误码、枚举、权限和内部状态替换为用户结果、必要原因和下一步；
6. 检查安全、费用、隐私、责任和必要授权披露没有因精简而缺失；
7. 修正后重新完成主要任务，确认视觉收敛未破坏功能和可访问性；
8. 在完成报告中记录页面、角色、视口和收敛结果。

未实际查看目标页面时，只能报告代码实现和自动化验证完成，不得报告产品验收完成。

## Admin v2 运营后台设计系统

### 产品定位

Admin v2 是面向平台和运营公司工作角色的持续任务系统，不是 App 的桌面放大版，也不是服务、接口、状态机和数据库的技术控制台。

产品气质冻结为：

- 清醒：优先展示当前任务、对象状态和需要作出的判断；
- 克制：正常状态不持续使用警告、风险横幅和证明型说明；
- 可靠：相同对象、状态和操作在所有业务域使用一致结构；
- 行动导向：每个异常、限制、等待和冲突都必须给出明确下一步；
- 可追溯：重要操作结果和审计记录可以查证，但审计信息不抢占主要任务。

### 唯一产品壳

- `ProductizedAdminShell` 是 Admin v2 唯一正式产品壳；
- `LegacyShell` 仅作为历史兼容实现，不得继续新增产品页面、视觉模式和业务入口；
- Stage One 至 Stage Five 仅作为阶段验证实现，不得作为正式沙箱、视觉回归或后续产品化基线；
- 正式沙箱构建必须同时启用多组织、后台认证和角色权限矩阵，并进入 `ProductizedAdminShell`；
- 删除历史壳必须先完成路由、能力、契约和测试映射，不得直接删除仍被测试或门禁引用的实现；
- 同一环境不得向使用者同时提供多个不同后台预览地址；失效预览必须停止或明确标记为历史实例。

### 导航领域

Admin v2 冻结十个一级业务域：

1. 工作台；
2. 成员与权限；
3. 运营公司；
4. 车主与车辆；
5. 行程运营；
6. 客服与安全；
7. 财务与对账；
8. 数据与报表；
9. 高层驾驶舱；
10. 审计与系统。

一级导航按当前工作身份的任务和权限裁剪：

- 无权限的领域不展示；
- 已授权但尚未实施的领域只显示统一不可用状态，不展示假数据和无效操作；
- 不以隐藏按钮代替服务端权限校验；
- 平台身份与运营公司身份不得合并权限或数据范围；
- 切换工作身份后必须重新加载导航、列表状态和数据范围。

### 角色任务模式

后台角色按任务模式组织设计，不为每个角色建立独立视觉系统：

| 任务模式 | 代表角色 | 默认任务 |
| --- | --- | --- |
| 账号治理 | 平台账号管理员、运营公司账号管理员 | 邀请、停用、角色范围和访问审计 |
| 综合运营 | 运营专员、运营负责人 | 任务分派、运营公司和行程协作 |
| 准入审核 | 车辆审核员、高级审核员、运营公司运力专员 | 车主资格、车辆材料和复核 |
| 客服安全 | 客服、安全专员、安全负责人 | 案件受理、限制、申诉和恢复 |
| 财务运营 | 财务经办、财务负责人 | 结算、对账、异常确认和双人复核 |
| 治理分析 | 隐私合规、数据分析、审计、技术运维 | 报表、审计、调查和受控数据访问 |
| 决策管理 | 平台决策人、运营公司负责人 | 风险摘要、待决事项和趋势判断 |

每种任务模式必须定义默认落点、高频筛选、对象详情、允许操作、危险操作、冲突恢复和返回列表上下文。

### 应用壳与滚动

桌面端基础尺寸冻结为：

| 区域 | 尺寸 |
| --- | --- |
| 左侧导航 | `248px` |
| 顶部栏 | `72px` |
| 内容区外边距 | `30px` |
| 常用控件高度 | `40–44px` |
| 主要操作高度 | `44–46px` |
| 标准表格行 | `52–60px` |
| 详情双栏间距 | `18px` |
| 标准卡片内边距 | `18–24px` |

滚动规则：

- `1280×800` 和 `1440×900` 下应用壳固定为视口高度；
- 浏览器页面本身不得出现非预期横向或纵向滚动；
- 左侧导航和顶部栏保持稳定；
- 长列表、长详情和报表在 `.product-content` 内部滚动；
- 表格只在内容区内横向滚动，不得撑宽应用壳；
- 审计摘要、编号、哈希和其他不可分隔字符串必须换行，不得撑宽详情卡片；
- 返回列表后恢复搜索、筛选、分页和内容区滚动位置。

### 信息密度

Admin v2 使用三档密度：

| 密度 | 使用场景 | 规则 |
| --- | --- | --- |
| `comfortable` | 登录、身份选择、空态和低频设置 | 单列、较大留白、一个主要行动 |
| `standard` | 工作台、案件、车主车辆和普通业务列表 | 页面标题、筛选、列表和详情形成稳定扫描路径 |
| `dense` | 审计、报表、财务和高频批量队列 | 提高行密度，但不得缩小焦点目标、隐藏必要标签或堆叠操作按钮 |

页面不得自行创建第四种密度。密度变化只能由共享组件属性控制。

### 页面构图

列表页面顺序冻结为：

1. 页面标题与角色上下文；
2. 必要的业务摘要；
3. 搜索、筛选、排序和已保存视图；
4. 列表或表格；
5. 分页和结果范围。

详情页面顺序冻结为：

1. 返回来源；
2. 对象名称、业务状态和必要上下文；
3. 影响当前判断的核心事实；
4. 当前角色允许执行的操作；
5. 操作结果、冲突或恢复；
6. 审计记录和关联对象。

不得在首屏同时展示全部历史、权限、审计、潜在风险和内部状态。

### 共享组件

后续 Admin v2 页面必须优先使用以下共享能力：

- `AdminAppShell`
- `AdminSidebar`
- `AdminTopbar`
- `WorkIdentityMenu`
- `GlobalSearch`
- `AdminPageHeader`
- `FilterBar`
- `SavedViewSelector`
- `MetricSummary`
- `DataTable`
- `TaskQueue`
- `MasterDetailWorkspace`
- `DetailSection`
- `StatusBadge`
- `TaskOwnership`
- `AuditTimeline`
- `InlineFeedback`
- `PageState`
- `EmptyState`
- `Pagination`
- `ConfirmDialog`
- `DangerDialog`
- `OperationUnknownState`

同一用途不得在不同业务域重复实现不同尺寸、颜色、间距或交互顺序。

### 表格与列表

- 搜索、筛选、排序和分页在所有列表使用相同顺序；
- 搜索框必须说明搜索对象，不使用泛化的“请输入关键词”；
- 默认排序应匹配角色任务，例如截止时间、风险优先级或最近更新；
- 状态列使用业务语言和共享状态色，不展示内部枚举；
- 每行只保留一个进入详情的主要入口；
- 高频操作进入详情或明确的行内操作区，危险操作不得直接散落在整行；
- 空态说明当前没有什么，仅在用户可以改变结果时提供行动；
- 加载和刷新不清空已有可读内容，未知结果必须提供查询确认入口。

### 状态与反馈

- 页面级加载、失败和空态使用统一 `PageState`；
- 局部操作结果使用 `InlineFeedback`，不重复叠加全局 Toast；
- 成功结果说明已经发生的业务变化；
- 等待结果说明由谁继续处理以及当前角色是否需要行动；
- 权限不足只说明当前不能完成的操作和可用出口，不展示权限代码或角色矩阵；
- 结果确认中禁止重复提交；
- 结果未知时不得显示成功或失败，必须允许查询最终状态；
- 恢复后旧警告、失败和阻断说明必须消失。

### 技术信息和审计例外

普通运营、客服、审核、财务和决策页面不得直接展示：

- API 字段、枚举、错误码和服务名称；
- 数据库、队列、命令、模块和策略实现；
- 请求关联编号、访问决策编号和完整哈希；
- 只用于证明系统已实现的审计、权限和门禁说明。

仅“审计与系统”中的授权审计或技术运维详情可以展示履职必需的技术证据，并且必须：

- 使用业务标题解释事件；
- 将原始字段放入次级证据区；
- 对长编号和哈希执行换行；
- 明确字段用途，不用原始枚举替代业务结果；
- 按角色和数据等级限制访问；
- 不允许通过技术调查改变行程、安全、资金或资格业务状态。

### 色彩与排版

- 后台使用系统无衬线字体，数据、表格和操作界面不使用装饰性衬线体；
- 页面标题使用 `32/40`，区块标题使用 `20/28`，正文和表格使用 `13–16px`；
- 品牌蓝只用于主要行动、当前导航和可交互焦点；
- 绿色只用于已经确认的完成结果；
- 琥珀色只用于需要关注、等待或即将到期；
- 红色只用于危险、冻结和不可逆操作；
- 中性信息不得使用风险色；
- 状态不能只依赖颜色，必须同时提供文字；
- 页面和业务组件不得继续增加散落的十六进制颜色，后续批次必须逐步迁移到主题令牌。

### 弹层与键盘

所有 Dialog、抽屉和全局搜索必须满足：

- 打开后焦点进入标题、首个输入或安全的次要操作；
- Tab 和 Shift+Tab 在弹层内循环；
- 背景导航、列表和详情进入 `inert`；
- 未提交时 Escape 可以关闭；
- 确认提交中 Escape 不关闭；
- 关闭后焦点恢复到触发元素；
- 确认中主要按钮禁用并展示进行中文案；
- 不允许重复提交；
- 状态变化通过 `status` 或 `alert` 播报。

### 冻结样板

以下三张后台样板继续作为正式冻结基线：

1. 后台认证壳；
2. 角色工作台壳；
3. 主从工作区及确认 Dialog。

后续组件提取和页面产品化不得自行简化、重排或改变这些样板已经冻结的身份选择、全局搜索、焦点陷阱、背景 `inert`、焦点恢复和确认中防重复行为。

### 验收视口

Admin v2 用户可见变更至少检查：

- `1280×800`；
- `1440×900`。

验收必须覆盖：

- 页面级横向和纵向溢出；
- 内容区内部滚动；
- 长字符串和表格宽度；
- 明暗主题；
- 全局搜索；
- 身份选择；
- 列表进入详情及返回恢复；
- 空态、加载态、失败态和未知结果；
- Dialog 焦点、背景 `inert`、Escape 和焦点恢复；
- 控制台 JavaScript 错误；
- 目标角色可见菜单、字段和操作。
