# 产品语言与界面最终态

本文档是 PollyCar 用户端与运营后台的用户可见语言、界面最终态和产品化验收真相源。业务状态、权限、安全和数据契约仍以 `spec/` 为准；视觉令牌、组件和跨页面布局以 `DESIGN.md` 为准。

本文件解决一个明确问题：系统必须实现大量技术、安全和边界规则，但界面不能因此变成需求说明、测试面板或内部状态展示器。

## 冲突优先级

1. `AGENTS.md` 定义不可绕过的工程与产品化硬约束；
2. 本文档定义哪些信息可以进入界面以及如何表达；
3. `DESIGN.md` 定义视觉层级、组件、信息密度和交互表现；
4. `spec/` 定义内部状态、权限、接口、错误码和数据载荷；
5. 代码和测试证明内部规则真实生效。

内部契约与用户表达发生冲突时，不得修改或弱化业务规则；应建立明确的展示映射。

## 最终态定义

一个页面只有同时满足以下条件，才可以视为产品最终态：

- 用户无需阅读项目文档即可知道自己在哪里、当前能做什么以及下一步是什么；
- 页面围绕目标角色的真实任务组织，而不是围绕接口字段、领域实体或验收条目组织；
- 正常路径突出主要任务，异常和限制仅在实际发生且用户可处理时出现；
- 技术状态通过业务行为和测试证明，不通过可见卡片、徽章、横幅或说明文字证明；
- 页面不存在只服务于开发、测试、评审或决策过程的解释；
- 临时占位文案、合成数据说明、机器状态和调试入口不会进入生产展示；
- 必要的费用、安全、责任、隐私和授权披露保持准确、及时且可理解；
- 用户可以从失败、等待、权限不足和未知结果状态中找到明确恢复路径。

## 用户可见性判断

任何信息进入普通业务界面前，必须依次通过以下判断：

1. 目标用户现在是否需要知道？
2. 这条信息是否会改变用户的决定或下一步操作？
3. 用户是否能够根据这条信息采取行动、等待或理解重要后果？
4. 是否存在更短、更自然、与任务更接近的表达？
5. 是否已经在标题、状态、按钮或上下文中表达过？

任一信息不满足前三项，默认不展示。第五项为“是”时，默认删除重复表达。

## 产品语言原则

- 先说结果，再说原因，最后给下一步；
- 使用目标角色熟悉的业务词，不使用实现词；
- 对失败保持诚实，但不暴露内部风控、权限结构和系统拓扑；
- 对等待说明当前进度和用户是否需要行动，不编造完成时间；
- 对不可用状态说明影响范围，不用大段文字重复平台规则；
- 对安全、费用和责任保持准确，不用“轻松”“快速”“无忧”等词弱化真实风险；
- 一个句子只承担一个任务，按钮使用动作而不是状态；
- 用户无需处理的正常事实不做成横幅、警告或状态卡片。

## 内部状态转换规则

内部状态不得直接渲染。每个展示映射至少包含：

| 字段 | 说明 |
| --- | --- |
| 用户标题 | 用户当前看到的结果或任务 |
| 必要说明 | 仅保留影响决定或行动的原因 |
| 下一步 | 可执行动作、等待方式或联系入口 |
| 展示条件 | 何时出现以及何时消失 |
| 披露等级 | 普通提示、重要提醒或强制确认 |
| 内部引用 | 对应状态、错误码或权限，仅供代码和测试使用 |

推荐在代码中集中维护展示模型，不允许组件直接显示原始枚举、错误消息或服务端内部说明。

### PollyCar 常用转换

| 内部事实 | 禁止直接使用 | 推荐用户表达 |
| --- | --- | --- |
| 车主资格尚未满足 | `DRIVER_INELIGIBLE`、资格校验失败 | 完成车主认证后即可发布或接受行程 |
| 车辆审核处理中 | 审核状态为 `pending` | 车辆资料正在审核；当前无需操作 |
| 需要补充材料 | 材料完整性断言失败 | 还需要补充以下资料 |
| 接单配额达到限制 | 已触发滚动配额策略 | 今天暂时无法接受更多行程；说明何时可再次尝试 |
| 权限不足 | 缺少 `permission`、角色校验未通过 | 你当前无法执行此操作；提供返回或申请协助入口 |
| 会话过期 | Token 失效、内部会话撤销 | 登录状态已失效，请重新登录 |
| 服务暂不可用 | 服务降级、Adapter 失败 | 暂时无法完成操作，请稍后重试 |
| 请求结果未知 | 幂等状态未知、网络写入不确定 | 暂时无法确认是否成功；先刷新结果，不要重复提交 |
| 功能门禁关闭 | 功能未启用、生产禁止启用 | 若用户没有可执行入口则不展示；有入口时说明当前不可用及替代路径 |
| 审计已记录 | 操作已写入审计日志 | 普通用户不展示；运营角色仅在履职需要时展示操作记录 |
| 合成数据或内部沙箱 | 当前为内部生产级沙箱 | 非生产环境仅保留一个统一全局标识，页面正文不重复解释 |

## 页面任务简报

新增页面或重构主要页面前，必须先在任务描述、代码附近的现有测试语义或实施对话中明确以下内容，不要求为普通任务新建独立文档：

```text
目标角色：
用户进入页面时要完成的任务：
唯一主要行动：
必须理解的重要后果：
可能出现的一个主要阻断：
阻断发生后的下一步：
不得出现在页面中的内部信息：
```

如果无法明确“唯一主要行动”，应先收敛页面职责，不得使用更多卡片和说明文字掩盖信息架构问题。

## 页面信息预算

普通用户页面默认遵循以下预算：

- 一个页面主标题；
- 一个当前阶段的主要任务；
- 一个强调级主要操作；
- 最多一个需要用户立即处理的主要阻断；
- 最多一个补充说明区域；
- 正常状态不展示安全、资格、配额、权限和系统健康的汇总面板；
- 同一事实不得同时通过标题、徽章、卡片、横幅和正文重复表达。

运营后台允许更高信息密度，但必须围绕角色任务、待处理事项、业务结果和风险处置组织，不得围绕服务、接口、命令和数据库实体组织。

## 状态表达模式

### 加载

- 说明正在加载的用户对象，例如“正在加载行程”；
- 不展示请求、同步、查询、接口和服务名称；
- 短暂加载优先使用局部骨架或进度反馈，不使用整页技术说明。

### 空状态

- 解释当前没有什么；
- 仅在用户可以改变结果时提供行动；
- 不把“返回空数组”“暂无合成记录”或过滤条件断言作为文案。

### 失败

- 标题说明用户任务未完成；
- 正文只解释可披露原因和影响；
- 提供重试、修改、返回、等待或求助动作；
- 不显示原始错误码、异常消息、堆栈、请求标识或内部责任归属。

### 等待

- 说明谁在处理以及用户是否需要行动；
- 未有可靠服务承诺时不得给出具体完成时间；
- 状态变化后自动移除旧说明。

### 限制与权限

- 只在用户触发相关动作或限制实质影响当前任务时展示；
- 说明当前不能做什么以及可以做什么；
- 不展示角色矩阵、权限名称、策略计算和风控阈值。

### 安全、费用与责任

- 在用户做决定前展示，不得埋在操作完成后的说明中；
- 使用准确业务语言，明确影响、金额、责任和撤销条件；
- 不因追求简洁而隐藏必要披露；
- 不披露会削弱安全控制、风控效果或他人隐私的内部细节。

## 禁止的证明型界面

以下模式默认视为未完成产品化：

- 为每个需求或状态建立一张卡片；
- 使用大量 `Badge` 展示内部资格、权限和状态机节点；
- 页面首屏长期显示沙箱、同步、服务、门禁或实现说明；
- 用“已满足”“已校验”“已记录”“已审计”证明系统行为；
- 把测试步骤、验收断言或接口响应改写成帮助文字；
- 同时展示正常状态、所有潜在异常和全部边界规则；
- 用户无法处理的信息仍以警告或错误样式出现；
- 用“当前业务能力保持关闭”等内部治理口径代替用户可理解的影响和下一步。

## 产品化收敛流程

用户可见界面完成业务实现后，必须单独执行一次产品化收敛：

1. 以目标角色进入页面并完成主要任务；
2. 检查正常态是否被异常、限制或工程说明抢占；
3. 检查每段文字是否影响理解、决定或操作；
4. 删除重复标题、规则复述、状态断言和无法行动的信息；
5. 将原始状态、错误和权限结果替换为集中展示映射；
6. 检查主要按钮是否使用用户动作；
7. 检查错误、空态、等待和恢复是否给出明确下一步；
8. 检查必要安全、费用、责任和隐私披露没有被误删；
9. 在代表性视口重新截图或目视检查；
10. 修正后再次完成主要任务，确认产品化没有破坏行为。

## 截图与文案验收

### 用户 App

- 至少检查一个当前主要支持的移动视口；
- 检查首屏、主要操作完成后状态以及一个最重要的失败或等待状态；
- 确认没有页面级横向溢出、内容遮挡、重复环境说明和技术词泄漏；
- 确认用户可以在不阅读说明文档的情况下完成主要任务。

### 运营后台

- 至少检查一个桌面视口；
- 涉及响应式布局、窄窗口或移动验收时，再检查一个窄视口；
- 使用目标角色账号检查导航、可见操作、权限拒绝和恢复路径；
- 确认业务页面使用业务语言，技术诊断只存在于授权运维入口。

### 文案删除审查

逐项询问：

1. 删除这段文字后，用户是否仍能完成任务？
2. 这段文字是否只是在重复标题、按钮或视觉状态？
3. 这段文字是否只对开发者、测试者或评审者有意义？
4. 用户看到后能否采取行动或理解重要后果？
5. 是否可以用更短的结果、原因和下一步表达？

前三项任一为“是”且第四项为“否”时，默认删除。

## 完成报告要求

用户可见界面变更的完成报告必须在“验证”中说明：

- 实际检查的页面和目标角色；
- 实际检查的设备或视口；
- 检查的正常态、失败态、空态或等待态；
- 删除或改写了哪些技术、断言或边界文案；
- 仍未完成的产品验收和残余风险。

自动化测试、类型检查和构建不能替代该产品化验收。

## Admin v2 运营后台语言

### 基本句式

后台页面围绕“对象、结果、行动”表达：

- 对象：当前处理的是哪个任务、行程、案件、车主、车辆、运营公司或财务记录；
- 结果：对象目前处于什么业务状态，对目标角色有什么影响；
- 行动：当前角色可以执行什么、需要等待什么或应交给谁继续处理。

页面标题不得使用服务名、模块名、命令名、接口名或数据库实体名。

### 工作台

- 标题使用角色任务，例如“平台安全负责人工作台”；
- 摘要只展示当前角色可以处理或需要判断的事项；
- 不使用“能力总览”“系统健康”“权限概览”证明后台能力；
- 没有任务时使用“当前没有需要处理的任务”，不使用“暂无合成任务数据”。

### 列表

- 搜索框说明搜索对象，例如“搜索行程、乘车人或车主”；
- 筛选项使用业务状态，例如“处理中”“等待复核”“已完成”；
- 不展示 `processing`、`waiting_review`、`resolved_upheld` 等内部枚举；
- 空态说明没有符合当前条件的对象，并允许清除或调整筛选；
- 加载失败说明哪个列表未能读取，并提供重新加载；
- 返回列表后保持搜索、筛选、排序和分页，不要求角色重新建立上下文。

### 详情

- 标题使用对象名称、路线、案件主题或业务编号；
- 首屏优先展示影响判断的事实和当前行动；
- “当前角色允许操作”只在确有操作或限制时出现，不作为所有详情页的固定证明区；
- 审计记录、访问范围和技术证据放在次级区域；
- 不使用“已校验”“已满足”“已记录”“已审计”证明系统行为。

### 操作

按钮使用明确业务动作：

- “分派任务”；
- “请求补充资料”；
- “暂停运营”；
- “恢复参与资格”；
- “提交复核”；
- “记录治理意见”。

不得使用：

- “执行命令”；
- “提交状态”；
- “调用服务”；
- “触发流程”；
- “更新记录”；
- “写入审计”。

### 操作结果

- 确认中：`正在确认结果，请勿重复提交`；
- 已完成：说明已经发生的业务变化；
- 失败：说明当前动作未完成，并提供重试、修改或返回；
- 未知结果：`暂时无法确认操作结果`，提供“查询最新结果”；
- 冲突：说明对象已被其他人更新，并提供重新加载；
- 权限不足：说明当前工作身份不能完成此操作，并提供返回或切换工作身份的出口。

操作完成后不得同时显示成功 Toast、结果横幅、状态卡片和审计证明。

### 审计与技术信息

普通运营页面不得显示：

- API 字段和错误码；
- 原始状态枚举；
- 服务、数据库、队列和模块名称；
- 完整请求标识、访问决策编号和哈希；
- “幂等”“策略命中”“权限校验通过”等实现说明。

授权审计和技术运维页面可以展示履职必需的技术证据，但必须先提供业务解释：

- `access_allowed` → `访问决策允许`；
- `access_denied` → `访问决策拒绝`；
- `authorized` → `已按当前访问范围允许`；
- `append_only` → `记录不可覆盖或删除`。

原始值只作为次级证据，不得替代页面标题、状态和行动。

### 高风险行动

涉及安全限制、运营暂停、资格恢复、资金确认、权限变更和数据导出时，确认 Dialog 必须按以下顺序表达：

1. 即将执行的业务动作；
2. 影响哪些对象和范围；
3. 是否可撤销；
4. 必要的责任、费用、隐私或安全后果；
5. 确认和取消操作。

不得在确认 Dialog 中复制状态机、策略条件或审计实现。
